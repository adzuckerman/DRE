<apex:page controller="Litmos.CreateAccountTeam" action="{!createLitmosTeam}">
        <style>
    input.btn.sft {
        display:block;
        float:right;
        height: 40px;
        padding: 0 6px;
        font-size:12px;
        border: #DEDEDE 1px solid;
        background: rgb(150,201,61);
        color: white;
            margin-right: 1%;
    }
    .message{
        width: 80% !important;
    }
</style>
     <apex:pageMessage summary="The Account was succesfully created as a Team in Litmos." severity="CONFIRM" strength="3" />
      <apex:pageMessages />
    <apex:form >
        <apex:commandButton value="Back" styleClass="sft" action="{!back}" style="background: rgb(150,201,61);color: white;margin-right:1%" />
    </apex:form>
</apex:page>